#Largest sum contiguous subarray (Flipkart)
a=[-2,-3,4,-1,-2,1,5,-3]
n=len(a)
curr_sum=a[0]
max_sum=a[0]
for i in range(1,n):
    curr_sum=max(a[i],curr_sum+a[i])
    max_sum=max(curr_sum,max_sum)
print(max_sum)
